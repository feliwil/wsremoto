package decisao;

import javax.swing.JOptionPane;

public class Exercicio1 {

	public static void main(String[] args) {
		int num1 = Integer.parseInt(JOptionPane.showInputDialog("Digite o numéro 1:"));
		int num2 = Integer.parseInt(JOptionPane.showInputDialog("Digite o número 2:"));
		String operacao = JOptionPane.showInputDialog("Digite qual operação aritmetica deseja: (+, -, * ou /:)");

		if (operacao!="/" && num2!=0) {
			if (operacao == "+") {
				float resultado = num1 + num2;
				System.out.println("O resultado da operação é: " + resultado);

			}else if (operacao == "-"){
				float resultado = num1 - num2;
				System.out.println("O resultado da operação é: " + resultado);		

			}else if (operacao=="*") {
				float resultado = num1 * num2;
				System.out.println("O resultado da operação é" + resultado);
			}else {
				float resultado= num1 / num2;
				System.out.println("O resultado da operação é:" + resultado);
			}

		}else {
			System.out.println("Impossivel realizar sua conta");
		}
	}

}